const login_state = (state) => state.login;
const register_state = (state) => state.register;
const feedback_state = (state) => state.feedback;
const receiver_state = (state) => state.receiver;
const loader_state = (state) => state.loader;

export default {
  login_state,
  register_state,
  feedback_state,
  receiver_state,
  loader_state,
};
